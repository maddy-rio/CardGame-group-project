<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <style>
        /* Hide the form by default */
        #gameRoomForm {
            display: none;
            position: absolute;
            top: 60px; /* Adjust this value based on your layout */
            background-color: #f9f9f9;
            border: 1px solid #ccc;
            padding: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>

<body>
    <div>
        {{#if user}}
        Welcome, {{user.name}}! You are logged in.
        <a href="/logout">Logout</a>
        <!-- Added Create Game Room button -->
        <button onclick="toggleGameRoomForm()">Create Game Room</button>
        <!-- Pop-down form -->
        <div id="gameRoomForm">
            <form action="/createGameRoom" method="post">
                <label for="roomName">Room Name:</label>
                <input type="text" id="roomName" name="roomName" required><br><br>
                <label for="roomPassword">Password:</label>
                <input type="password" id="roomPassword" name="roomPassword"><br><br>
                <input type="submit" value="Create">
            </form>
        </div>
        {{else}} You are not logged in.
        <a href="/login">Login</a>
        {{/if}}
    </div>

    <script>
        function toggleGameRoomForm() {
            var form = document.getElementById("gameRoomForm");
            if (form.style.display === "none") {
                form.style.display = "block";
            } else {
                form.style.display = "none";
            }
        }
    </script>
</body>

</html>
