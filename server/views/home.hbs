<body>
  <div>
    {{#if user}}
      Welcome,
      {{user.name}}! You are logged in.
      <a href='/logout'>Logout</a>
    {{else}}
      You are not logged in.
      <a href='/login'>Login</a>
    {{/if}}
  </div>

</body>